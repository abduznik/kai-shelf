name: Build (reusable)

on:
  workflow_call:
    inputs:
      build_mode:
        description: "Flutter build mode (debug or release)"
        type: string
        default: release

jobs:
  build-android:
    name: Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/gradle/wrapper/gradle-wrapper.properties', '**/*.gradle*') }}

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --${{ inputs.build_mode }}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: kai-shelf-android-${{ inputs.build_mode }}
          path: build/app/outputs/flutter-apk/*.apk

  build-windows:
    name: Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows
        run: flutter build windows --${{ inputs.build_mode }}

      - name: Zip build output
        shell: pwsh
        run: Compress-Archive -Path "build/windows/x64/runner/${{ inputs.build_mode == 'debug' && 'Debug' || 'Release' }}/*" -DestinationPath "kai-shelf-windows-${{ inputs.build_mode }}.zip"

      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: kai-shelf-windows-${{ inputs.build_mode }}
          path: kai-shelf-windows-${{ inputs.build_mode }}.zip

  build-web:
    name: Web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build web
        run: flutter build web --${{ inputs.build_mode }}

      - name: Zip build output
        run: (cd build/web && zip -r ../../kai-shelf-web-${{ inputs.build_mode }}.zip .)

      - name: Upload web build
        uses: actions/upload-artifact@v4
        with:
          name: kai-shelf-web-${{ inputs.build_mode }}
          path: kai-shelf-web-${{ inputs.build_mode }}.zip
